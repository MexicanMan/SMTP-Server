Autogen Definitions fsm;

method = case;
type   = reent;
prefix = client_fsm;

state  = initialized, s_ehlo, s_mf, s_rt, s_data, s_mail, s_quit, timeout, error;
event  = connected, ehlo, mf, rt, data, quit, connection_timeout, connection_lost;

transition =
{ tst = init; tev = connected; next = initialized; ttype = connect; };

transition =
{ tst = initialized;          tev = ehlo;             next = s_ehlo;           ttype = ehlo; };